# Changelog

## 0.1.7

- Document the six native hook events, Korean display names, feature dependencies and exact coverage boundaries; add a portable hook configuration example.
- Provide an isolated whole-suite test command and Linux/macOS CI configuration for source checkouts.
- Include public security, attribution, contribution, visual and release documents in the package manifest.
- Explain the open-source contribution, existing evidence and a benchmark plan without asserting unmeasured savings or adoption.
- Publish the original Gateway privacy/live feature-switch changes as a separate patch with the upstream MIT notice and exact base commit; no upstream distribution is bundled or modified automatically.
- Add synthetic regressions for the 85% telemetry threshold, compaction boundaries and isolated test-runner environment.
- Release preparation on the maintainer's Mac: isolated `npm test` passed 146/146 tests; `npm pack --dry-run --json --ignore-scripts` contained all required public files. CI status is reported independently by GitHub Actions. The upstream Gateway suite was not rerun for this patch export.

## 0.1.6

- Add structured visual measurement and finite Jev candidate review, optional hash-bound design-token application, and visual routing consultation for recognized native hook calls.
- Keep pixel inspection unperformed, preserve valid uncertain confidence, and retain the host's approval policy.
- Related checks passed: 60 tests before the uncertainty-preservation correction and 17 affected review/CLI tests after it. Counts overlap and are not additive.
