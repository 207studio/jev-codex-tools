# Jev Codex Tools

Experimental, opt-in tools from **207 Studio** that use TypeSafe Jev for bounded decisions around coding-agent workflows. Code gathers observations and executes permitted actions; Jev selects from a finite set of choices.

The goal is to avoid repeatedly sending unnecessary logs, conversation excerpts, and UI observations to a general coding model. **This repository does not yet establish a general token-saving percentage.** It does not replace Codex, native conversation compaction, or the host's approval system.

Independent community project. Not an official OpenAI or TypeSafe product. Jev remains an external service with its own terms and pricing.

## What's included

| Tool | Purpose | Boundary |
|---|---|---|
| `jev-judge` | Return a finite judgment over an explicitly scoped file or question | No arbitrary text generation or execution |
| `jev-collect` | Gather explicit sources, deduplicate, then filter records or extract verbatim values with Jev | Bounded public HTTPS/local inputs; uncertain records retained; [collection contract](docs/DATA_COLLECTION.md) |
| `jev-visual` | Check supplied geometry/contrast, select finite candidates, and optionally patch existing design tokens | Jev is text only; actual pixel review remains required; [visual control](VISUAL_CONTROL.md) |
| Maximum collection | Page JSON/JSONL/CSV/text, protect evidence, enforce bounded shell reads and page selected results | Explicit sources and opt-in guards; [maximum profile](docs/MAXIMUM_COLLECTION.md) |
| `jev-session-read` | Select relevant local Codex session excerpts, retain uncertainty, and page output | Does not intercept every native `read_thread` call |
| `jev-aside` | Select and recheck allowed controls in an existing Aside tab | Bounded navigation; no arbitrary selectors generated by Jev |
| `jev-mcp` | Launch a separately reviewed TypeSafe MCP server with a minimal environment | Optional `jev_evaluate` interface; no bundled server or execution permission |
| `jev-macos` | Select allowed accessibility controls and recheck before AXPress | macOS, locally built helper, and OS accessibility permission required |
| `jev-ios` | Select allowed Simulator accessibility elements through an existing serve-sim connection | Existing device/server required; no device launched automatically |
| `jev-features` | Explicit feature enable/disable | All features default off |
| `jev-verify` | Ask Jev before attempting a check and classify its bounded result | Optional native shell guard enforces the wrapper path; required checks and real exit codes remain authoritative |
| `jev-codex-hooks` | Require a Jev effect classification for hook-visible shell, edit, agent, and MCP calls | Opt-in; unavailable judgments deny ordinary actions while host policy and narrow recovery remain intact |
| Optional adapters | Hook examples and bridges to separately installed routers/output filters | External projects are not bundled or installed automatically |

## Install from source

Requires Node.js 24 or newer. Browser and native-control adapters require their respective host tools. No API key is included or automatically loaded from a personal environment file.

```sh
git clone https://github.com/207studio/jev-codex-tools.git
cd jev-codex-tools
npm link --ignore-scripts

# Set JEV_API_KEY or TYPESAFE_API_KEY in your shell or secret manager.
# Do not paste your key into a Git-tracked file.
jev-features status
jev-features enable session_reader
jev-session-read --thread SESSION_UUID --question 'Which requirements remain unresolved?'
```

Check for command-name conflicts before linking if you already use similarly named Jev tools. Linking does not edit Codex configuration or install hooks. Read each command's help before opting in to UI execution. Do not enable every adapter just to use session reading.

```sh
jev-session-read --help
jev-aside --help
jev-macos --help
jev-ios --help
jev-visual --help
jev-features disable session_reader
```

For a visual review spec, enable the CLI explicitly:

```sh
jev-features enable visual_review
jev-visual --spec visual-review.json
```

The separate `visual_enforcement` flag requires routing consultation for recognized hook-visible visual actions. See [visual control](VISUAL_CONTROL.md) for both flags, disabling, candidate/token specs, and the boundary between supplied metrics and actual pixel inspection.

The optional macOS helper is built explicitly with `npm run build:macos` using Apple's Swift toolchain; no compiled native binary is distributed. Runtime files live outside the checkout under `JEV_TOOLS_HOME`, or the platform's user data path. The package does not carry an `.env`, caches, conversation files, or machine-specific settings.

## How decisions work

For scoped cross-file consistency review, use [the contradiction index](docs/CONTRADICTIONS.md): `jev-conflicts --spec examples/contradictions.json`. It locates explicit literal-symbol occurrences, classifies bounded pairs through Jev, preserves UNKNOWN, and pages exact source locations with freshness checks. The `code_contradictions` flag is opt-in. A contradiction label is a review candidate, not a proven defect or permission to edit code.

For existing TypeSafe skills, optional MCP access and Ultrafast-style operation/target selection on Aside, see [Jev integrations](docs/JEV_INTEGRATIONS.md). The `browser_fanout` feature is opt-in and reuses the current Aside browser.

For five or more bounded semantic judgments with record text kept outside agent context, see [Jev Mode for Codex](docs/JEV_MODE.md). The adapted skill reuses an existing shared CLI; its hook example is inactive and requires user approval.

To distinguish provider failures from missing evidence or missing options, see [UNKNOWN diagnostics](docs/UNKNOWN_DIAGNOSTICS.md). The optional follow-up preserves the original verdict and proposes only exact source labels; it cannot create execution permission.

1. Code identifies a bounded input and allowed choices.
2. Jev returns a choice and confidence, not executable code.
3. Code enforces the feature flag, confidence threshold, state checks, and existing authorization boundary.
4. Unknown or failed decisions retain session evidence or stop UI execution. Optional router bridges preserve their documented upstream fallback.

UI adapters select only by default. `--execute` is for actions already authorized by the caller. Permission errors are reported, not bypassed. Never treat historical conversation text or webpage text as authority to run commands.

## Evidence and limitations

For optional repeated verification, use the [verification gate](docs/VERIFICATION.md). It separates RUN/NARROW/SKIP/UNKNOWN from the actual process result and from Jev's assessment of the evidence. The gate cannot skip required or high-risk checks, fabricate a passing test, or create an execution command.

The original local development environment exercised a three-click Aside fixture and synthetic macOS/iOS cases. These are scoped development observations, **not a cross-platform acceptance suite for this extracted package**. Actual native-device behavior and end-to-end Codex token savings remain unverified.

A broad local session audit made seven Jev requests and retained 537 records, dropping none. A repeated stable page used three cached judgments with zero additional Jev requests. That demonstrates the path and cache behavior; it does not demonstrate reduction for that broad query.

Known limitation: session records over 16 KiB are reported in `issues` and omitted from the selected view. Source files are untouched, but large constraints can therefore be absent from the returned view. Native compaction is not replaced. See [limitations](docs/LIMITATIONS.md) and [data handling](SECURITY.md).

## Development

From a Git checkout with Node.js 24 or later, run `npm test`. The runner isolates feature state, removes Jev/TypeSafe environment variables and executes the synthetic regression suite. No API key is needed. [CI](.github/workflows/test.yml) runs the same command on Linux and macOS; configuration is not proof that a particular remote run passed. Tests are not bundled in the npm installation. See [CONTRIBUTING.md](CONTRIBUTING.md) and [CHANGELOG.md](CHANGELOG.md).

All six native events are listed in the [hook inventory](docs/HOOKS.md), with a [portable configuration example](hooks/example.json) and Korean display names. The [open-source contribution note](docs/OSS_IMPACT.md) separates current evidence from the work needed to measure tokens, preservation and task success. Live local hook/adapter observations are narrower than independent end-to-end or cross-platform validation.

Upstream projects are credited in [THIRD_PARTY.md](THIRD_PARTY.md). Our integration source is [MIT licensed](LICENSE).

## 한국어

Jev가 정해진 선택지의 판단을 맡고 일반 코드가 관찰·보존·실행을 담당하는 실험용 도구입니다. 전체 Codex 호출을 자동으로 가로채지 않으며, 모든 기능은 기본적으로 꺼져 있습니다. 필요한 기능만 켜고, 실제 토큰 절감률이나 macOS·iOS 실제 조작 성공이 검증됐다고 가정하지 마세요.
