Route these six synthetic cases. Do not execute their tasks or use any tools. Return only one JSON object mapping each supplied id to its strategy. Do not add explanation or confidence scores.
Allowed strategies: MAIN_DIRECT, MAIN_SCOPED, BATCH_JEV, CHILD_PROCEDURE, CHILD_IMPLEMENTATION, CHILD_DESIGN, UNKNOWN.
For raw metadata, apply these ordered rules: known lookup -> MAIN_DIRECT; at least five bounded semantic items -> BATCH_JEV; otherwise known non-mixed tasks with at least two independent units and parallel benefit -> a child matching the role (procedure, implementation, design); other known edits with known scope -> MAIN_DIRECT; other known tasks -> MAIN_SCOPED. For mixed tasks, use the short goal to distinguish procedural checklists from implementation/review or design under conflicting constraints. Children require at least two independent units and parallel benefit. Sequential mixed work stays MAIN_SCOPED. Insufficient information stays UNKNOWN.
Records may instead contain precomputed advisory strategies from code or Jev. In that case report the supplied allowed strategy and preserve UNKNOWN; do not invent missing evidence. These are routing choices only, never execution permission or test-success claims.
Records:
